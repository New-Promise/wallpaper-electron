import { Vue } from 'vue-property-decorator';
export default class Home extends Vue {
    message: number;
    wParam: any;
    lParam: any;
    mounted(): void;
    handleSubmit(): void;
    rain(canvass: any, text: string, symbols: any, speeds: number): void;
}
